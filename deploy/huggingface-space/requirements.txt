# HuggingFace Space dependencies — mock mode, CPU-only.
# Mirror of the main repo's [ml] + [eval] extras.

fastapi>=0.136.1
pydantic>=2.13.4
pydantic-settings>=2.14.1
structlog>=25.5.0
jinja2>=3.1.6
prometheus-client>=0.25.0
httpx>=0.28.1
sse-starlette>=3.4.4
pyyaml>=6.0.3

# ML / RAG (CPU torch wheel resolved automatically by HF Spaces)
torch>=2.12.0
transformers>=5.8.1,<6.0.0
sentence-transformers>=5.5.1,<6.0.0
chromadb>=1.5.9
numpy>=2.4.6,<3

# Demo UI — gradio 6.x ships updated client support with pydantic-v2 compatibility.
gradio>=6.14.0,<7
