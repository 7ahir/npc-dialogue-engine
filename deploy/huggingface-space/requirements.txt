# HuggingFace Space dependencies — mock mode, CPU-only.
# Mirror of the main repo's [ml] + [eval] extras, pinned for reproducibility.

fastapi>=0.111.0
pydantic>=2.7.0
pydantic-settings>=2.2.0
structlog>=24.1.0
jinja2>=3.1.0
prometheus-client>=0.20.0
httpx>=0.27.0
pyyaml>=6.0

# ML / RAG (CPU torch wheel resolved automatically by HF Spaces)
torch>=2.1.0
transformers>=4.40.0,<5.0.0
sentence-transformers>=2.7.0,<4.0.0
chromadb>=0.5.0
numpy<2

# Demo UI
gradio>=4.44.0
