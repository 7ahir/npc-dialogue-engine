"""CLI entry points for the NPC Dialogue Engine.

Each module exposes a ``main()`` function registered in ``pyproject.toml``
under ``[project.scripts]``. The same modules are runnable directly with
``python scripts/<name>.py`` for quick iteration.
"""
